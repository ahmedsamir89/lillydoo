{% extends 'content_base.html.twig' %}

{% block content_body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {% if address.picture is not empty %}
            <img src="{{ asset('uploads/address_images/' ~ address.picture) }}" width="25" height="25">
        {% endif %}
        <div style="margin-top: 10px">
            <button type="submit" class="btn btn-primary">Edit!</button>
        </div>
    {{ form_end(form) }}
{% endblock %}

